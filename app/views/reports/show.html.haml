%h3
  %strong #{@test.name}
  \ // Test Report
%hr

= render 'reports/summary'
= render 'reports/averages'
= bar_chart @questions.joins(:answers).group('text').average(:response)
-# {library: {hAxis: {title: "hih"}}, {vAxis: {title: "hiv"}}}
= render 'reports/comments'
