%h5#report-chart Average responses
%hr
.col.s12#avg-chart
  - if @data.any?
    = bar_chart @data, library: @library
  - else
    #chart-box.valign-wrapper
      %p.flow-text.valign#chart-box-text Sorry, no data yet!
