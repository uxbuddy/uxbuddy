%h2.center-align Your Tests
%table.bordered
  -@tests.each do |test|
    %tr.collection-item
      %td=test.name
      %td=link_to "View Report", test_report_index_path(test), class: "active"
    -# =link_to "View Report", report_path(test), class: "active"
